# Curriculum pivot: technical track on Ellen's real task app is now primary

The user steered the focus to a **technical skill track** grounded in Ellen's
**real, current project: a task app** — backend in **C#** (her strength + the
husband's), frontend in **Astro**. She is actively building it. This is the best
available vehicle: real, motivating, and touching deep transferable skills.

The technical spine (areas the user named):

- **Design** — UI/UX, frontend craft.
- **Architecture** — how the C# API + Astro frontend + DB fit; boundaries; data
  flow; where each piece lives.
- **Cloud deployment** — publish backend + DB to a real cloud. (She has shipped
  static sites, but not a real backend + DB to production — likely the biggest
  gap.)
- **DB tradeoffs** — choice of DB, schema design, read/write patterns,
  migrations.

## Priority shift

- **Primary now: technical depth via the task app.**
- **Parked** (later "business/sell" track): the baker/discovery work
  (LR-0007/0008) and the WhatsApp-dashboard idea (LR-0006). The EP marketing
  site (LR-0005) is also not the active vehicle.
- The mission north star (first paying customer) is unchanged; the immediate
  focus is Ellen's technical depth so she can later deliver real client work.

## Open (needed to design Lesson 1 — NOT assumptions)

- Task-app current state: backend working locally? Astro frontend built?
  connected (frontend calling the API)? anything deployed?
- C# backend specifics: ASP.NET Core? minimal API vs controllers? DB (SQL Server
  / Postgres / SQLite) + EF Core?
- **Is it in a repo the agent can inspect?** (Preferred — assess real ground
  truth, as was done for her sites, instead of guessing.)
- Which of design / architecture / cloud-deploy / DB does she most want/need
  next — or what is she stuck on this week?
