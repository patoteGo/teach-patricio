# Teaching Notes

## Learner profile

- Experienced developer. Skip generic "what is an LLM / terminal / git" framing entirely.
- Wants Pi as a **power tool**, not a chat assistant. Lead with mechanics, model, and customization — not "how to ask nicely".
- Fastest path to value = understanding the core loop + the extension/skill system.

## Stated preferences (respect these every lesson)

- **Graphical HTML.** Every lesson must be visually rich: diagrams, SVG, color, visual hierarchy. Avoid text walls. (User: "every time you create a html, make it as graphical as possible".)
- **Portable.** This whole folder is meant to become a git repo the user clones elsewhere. Keep artifacts self-contained and self-citing; no absolute local paths baked into lessons (cite web URLs + note local install path exists).
- Ponytail sensibility requested by harness config — teach the lazy/power path.

## Teaching approach decisions

- Sequence (proposed): (1) Core loop / mental model → (2) Interactive TUI mastery → (3) Sessions, branching, compaction → (4) Context files (AGENTS.md) → (5) Skills → (6) Extensions → (7) Packages & trust. Adjust based on what the user asks.
- Lessons are short, one tight win each, per the teach skill.
- Build reusable components into `assets/` only when a second lesson would duplicate them. Lesson 1 ships the shared stylesheet (`assets/style.css`).

## Session log

- Session 1: Established mission, populated RESOURCES.md, shipped Lesson 1 (the core loop). Created learning record 0001.
- Session 2: Shipped the **Pi glossary** (`reference/pi-glossary.html`, ~40 terms, 6 color-coded categories, click-to-filter) and **Lesson 2 — Driving the TUI** (screen anatomy SVG, power-keys map, steering vs follow-up vs abort with a delivery-timeline diagram, footer budget gauge, essential commands, quiz). Promoted the footer-label style to a shared `footer.lesson-foot .lbl` class in `assets/style.css` (deduped lesson 1+2, killed a multi-line-inline-style CSS blocker). Re-scoped Lesson 2 to the interactive control surface; branching/compaction now slated for Lesson 3.
- Session 3: Shipped **Lesson 3 — Sessions, Branching & Compaction**. Grounded in `sessions.md` + `compaction.md` (read in full). Diagrams: a two-branch session-tree SVG with active leaf + abandoned branch; the compaction trigger formula; a before/after SVG (summarize-old / keep-recent / what-the-LLM-sees / nothing-deleted). Covers `/tree` selection behavior, the `/tree` vs `/fork` vs `/clone` comparison, CLI session flags, branch summaries, and `compaction.*` + `treeFilterMode` settings. Lesson 4 slated for **context files (AGENTS.md / system prompt / .pi settings)**.
- Session 4: Shipped **Lesson 4 — Context Files (Teach Pi your codebase)**. Grounded in `settings.md` (read in full). Three-lever model (AGENTS.md / SYSTEM.md / settings.json); SVG of AGENTS.md concatenation (global→parents→current, all stacked); replace-vs-append system prompt; SVG of global+project settings merge (project wins, nested merged); trust as the security boundary (`/trust`, `defaultProjectTrust`, `-a`/`-na`); copy-paste starter `.pi/settings.json` + AGENTS.md template. Lesson 5 slated for **skills**.
- Session 5: Shipped **Lesson 5 — Skills (capability on demand)**. Grounded in `skills.md` (read earlier). Star concept = **progressive disclosure** (only name+description always in context; full SKILL.md loads on match) shown as a 3-stage SVG. Covers folder anatomy, SKILL.md frontmatter (description = the routing key, good vs poor examples), discovery locations + the `~/.agents` root-md quirk, auto vs `/skill:name` invocation, and a complete copy-paste `commit/` skill (SKILL.md + `scripts/diff-stat.sh`). Noted the "ask Pi to build a skill" cheat-code. Lesson 6 slated for **extensions**.
- Session 6: Shipped **Lesson 6 — Extensions (program Pi itself)**. Grounded in `extensions.md` (read Quick-Start through Custom Tools; lifecycle + `ctx` surface + registerTool + tool_call/tool_result interception). Centerpiece = a full **event-lifecycle SVG** with the block/mutate hooks (`context`, `tool_call`, `tool_result`) highlighted red. Covers the skill-vs-extension dividing line, custom tools (`registerTool` → `{content, details}`, typebox params, `promptSnippet`/`promptGuidelines`), the `ctx` surface, locations + trust + `-e`/`/reload`, and two copy-paste worked examples: a `deploy` custom tool and a **safety-gate** extension that blocks `rm -rf` via `tool_call` + `ctx.ui.confirm`. Lesson 7 slated for **packages**.
- Session 7: Shipped **Lesson 7 — Pi Packages (finale)**. Grounded in `packages.md` (read in full). Covers the bundle-of-4-primitives model (SVG), install/manage + 3 source types (npm/git/local), pinning vs floating + team reproducibility, creating a package (`pi` manifest + `pi-package` keyword + deps rules), **security as the headline** (review-source, trust gates, `-e` to try), filtering/`pi config`/dedup, and a **course-complete recap** (7-dot arc SVG) + wisdom pointers (Discord, gallery, skill repos) + a capstone call (build your dotfiles). **Planned 7-lesson arc COMPLETE.**
- Session 7 cleanup: a `refreshRunners:cheap` full scan surfaced CSS blockers the per-file scans had missed — root cause was **inline `style=` attributes containing quoted font names** (`&quot;JetBrains Mono&quot;` / `&quot;Space Grotesk&quot;`, plus single-quote time-bombs in L7 the formatter would later encode), which the CSS parser rejects. Fixed by adding a shared `.mono` utility class to `assets/style.css` and stripping every quoted-font inline style across lessons 2/3/5/6/7 + the glossary (dropped the redundant ones on `<code>`, already mono via CSS). **Whole course re-scanned: 0 errors across 9 files.**
- Session 8 (repo-wide reorg): **made the repo multi-topic.** Moved this entire Pi workspace into `topics/pi/` (all internal relative links preserved — `../assets/style.css`, `../MISSION.md`, etc. still resolve). Root is now a catalog: new `index.html` graphical hub + rewritten `README.md` with an auto-synced `<!-- topics:start -->` catalog block. Generalized `.github/scripts/sync_readme.py` to walk `topics/*/lessons/` (skips `_`-prefixed dirs) and `.github/workflows/sync-lessons.yml` to trigger on `topics/**`. Added `topics/_template/` (MISSION/RESOURCES/NOTES templates + a copy of the stylesheet + .gitkeep dirs) so new topics are one `cp -r` away. Gitignored `.ruff_cache`. New topic = `cp -r topics/_template topics/<name>` then edit MISSION + drop lessons; README catalog self-updates on push.
