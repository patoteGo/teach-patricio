# Teaching Notes — {Topic}

## Learner profile

- {Background, what to skip, what to emphasize.}

## Stated preferences

- {How this learner wants to be taught.}

## Interactive lesson contract

- Use `h2.section` for each real learning section; the shared UI automatically adds a persisted completion checkpoint.
- Put **one recall-worthy phrase** in `<strong>` (or `<em>`) per teaching paragraph; recall mode automatically masks those phrases for retrieval practice.
- Prefer native `<details><summary>question</summary>answer</details>` for optional explanations before adding custom JavaScript.
- Add bespoke quizzes/calculators only when they test behavior the shared recall/checkpoint tools cannot cover.
- Keep every learner-facing label paired with `data-lang="pt"` and `data-lang="en"`.
- Register every new lesson in the sidebar (`assets/nav.js`), the README lesson table, the hub card count in `index.html`, and the previous lesson's `concept-nav`. See root `AGENTS.md`.

## Session log

- Session 1: {what shipped, what's next.}
