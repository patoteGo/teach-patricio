# Teaching Notes — LocalStack & AWS

## Learner profile

- Experienced full-stack dev; already runs LocalStack in Docker.
- Wants CLI fluency (awslocal) + Terraform + testable infra.
- **Wants Python AND TypeScript side by side** in SDK/Lambda examples — the
  two-language comparison is a feature, not an accident. Keep it.

## Stated preferences

- Challenges must escalate easy → hard and be self-verifiable (expected output
  as feedback loop).
- Stick to services available in the Community/free tier; note licensed ones
  (RDS, ECR, API GW v2) instead of depending on them.

## Interactive lesson contract

- Use `h2.section` for persisted completion checkpoints.
- One recall-worthy phrase in `<strong>` per teaching paragraph.
- Every challenge ends with a `verify` command + expected output.
- Bilingual PT/EN labels; code comments in PT.

## Session log

- Session 1: Created the topic — 7 lessons (easy→hard), CLI cookbook reference,
  side-by-side Python/TS convention established from Lesson 1.
